
# A sample job for bakehouse_analytics.

resources:
  jobs:
    sample_job:
      name: bakehouse_data_refresh

      trigger:
        # Run this job every day, exactly one day from the last run; see https://docs.databricks.com/api/workspace/jobs/create#trigger
        periodic:
          interval: 1
          unit: DAYS

      #email_notifications:
      #  on_failure:
      #    - your_email@example.com

      parameters:
        - name: catalog
          default: ${var.catalog}
        - name: schema
          default: ${var.schema}

      tasks:
        - task_key: customer360_refresh
          notebook_task:
            notebook_path: ../src/customer_360.ipynb
            source: WORKSPACE
        - task_key: sales_transaction_refresh
          notebook_task:
            notebook_path: ../src/transaction_summary.ipynb
            source: WORKSPACE

      environments:
        - environment_key: default
          spec:
            environment_version: "4"
